{
  "version": 2,
  "buildCommand": "cd packages/landing && yarn build",
  "outputDirectory": "packages/landing/.next",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": "nextjs"
}
